<h1>顧客進捗登録</h1>
<%= form_with model: @history, local:true do |f| %>
  <h4>接触方法</h4>
  <%= f.select_field :action %>
  <h4>結果</h4>
  <%= f.select_field :result %>
  <h4>次の行動・メモ</h4>
  <%= f.text_area :introduction %>
  <%= f.submit '投稿' %>
<% end %>